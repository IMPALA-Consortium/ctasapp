% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_FieldDetail.R
\name{build_param_lookup}
\alias{build_param_lookup}
\title{Build display parameter lookup from measures}
\usage{
build_param_lookup(df)
}
\arguments{
\item{df}{Measures data frame.}
}
\value{
A data frame with \code{display_id}, \code{parameter_ids} (list column),
\code{plot_type}, and \code{cat3_values} (list column).
}
\description{
Groups parameters by \code{parameter_category_2} (the "field" key). This
naturally groups norm + missing-ratio labs under a single entry, and
categorical one-hot levels under their shared prefix. The \code{plot_type}
is determined by the dominant \code{parameter_category_3}; mixed types
(e.g. \code{range_normalized} + \code{ratio_missing}) are treated as \code{"numeric"}.
}
\details{
When multiple parameters share a \code{parameter_category_2} but have
identical \code{parameter_category_3} values that aren't meant to be grouped
(e.g. two independent numeric params with the same generic category),
they are kept separate using \code{parameter_id} as the display key.

Now also includes a \code{cat3_values} list column with raw \code{parameter_category_3}
values for icon determination.
}
\keyword{internal}
