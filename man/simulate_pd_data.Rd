% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_ctas.R
\name{simulate_pd_data}
\alias{simulate_pd_data}
\title{Simulate protocol deviation (SDTM DV) records for a ctas input dataset}
\usage{
simulate_pd_data(
  ctas_data,
  seed = 321,
  pds_per_site = c(1L, 5L),
  date_range = c("2023-01-01", "2024-12-31")
)
}
\arguments{
\item{ctas_data}{A ctas input list containing a \code{subjects} element with
\code{site} and \code{subject_id} columns.}

\item{seed}{Integer seed for reproducibility.}

\item{pds_per_site}{Integer vector of length 2 giving the inclusive
min/max number of PD rows to generate per site (uniform draw).}

\item{date_range}{Character vector of length 2 with the inclusive ISO
date range (\code{YYYY-MM-DD}) used to sample \code{dv_start_date}.}
}
\value{
A data frame with columns \code{site}, \code{subject_id}, \code{dv_seq},
\code{dv_term}, \code{dv_decod}, \code{dv_cat}, \code{dv_scat}, \code{dv_start_date},
\code{dv_end_date}.
}
\description{
Generates plausible protocol deviation rows for each site in
\code{ctas_data$subjects}. Deviations link at site level only — they are
not tied to a \code{parameter_id}. Columns follow lowercase snake_case
equivalents of the SDTM DV domain (see the CDISC SDTM Implementation
Guide, DV domain).
}
