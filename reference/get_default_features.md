# Get default features from configuration

Get default features from configuration

## Usage

``` r
get_default_features()
```

## Value

Character vector of default feature names, or NULL if all features
should be selected.
