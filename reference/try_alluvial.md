# Attempt to render an alluvial plot

Attempt to render an alluvial plot

## Usage

``` r
try_alluvial(df_plot)
```

## Arguments

- df_plot:

  Prepared data frame with val_cat, subject_id, site_label,
  timepoint_1_name.

## Value

A ggplot if successful, NULL if it errors.
