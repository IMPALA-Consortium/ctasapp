# Get built-in default configuration

Returns the hardcoded defaults that match the shipped
`inst/ctasapp.yml`. Used as fallback when keys are missing from a
user-supplied config.

## Usage

``` r
default_config()
```

## Value

A nested list.
