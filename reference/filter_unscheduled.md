# Filter out unscheduled visits

Filter out unscheduled visits

## Usage

``` r
filter_unscheduled(df)
```

## Arguments

- df:

  Data frame with a `timepoint_1_name` column.

## Value

Filtered data frame.
