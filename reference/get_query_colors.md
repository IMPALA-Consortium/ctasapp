# Get query dot colours from configuration

Get query dot colours from configuration

## Usage

``` r
get_query_colors()
```

## Value

A named list with `no_change` and `data_change` colour strings.
